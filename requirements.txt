# Scripts + benchmarks (install library first: pip install -e .)
# Core deps are declared in pyproject.toml for the ``dcee`` package.
numpy>=1.22
scikit-learn>=1.2
tqdm>=4.64
faiss-cpu>=1.7.4

# Text embeddings for --mode documents / --query (pulls PyTorch)
sentence-transformers>=2.2.0
torch>=2.0.0

# --- Optional: GPU acceleration (install ONE matching your CUDA toolkit)
# CUDA 12.x:
# cupy-cuda12x>=12.0
# CUDA 11.x:
# cupy-cuda11x>=11.0
#
# If you skip CuPy, ``dcee`` runs on NumPy (slower but fine for testing).
